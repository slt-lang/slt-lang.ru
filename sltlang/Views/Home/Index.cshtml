@{
    ViewData["Title"] = "SLThree";
    Locale locale = (Locale)ViewData["culture"];
    var all = locale.Strings.Where(x => x.Key.StartsWith("RootHome+")).OrderBy(x => x.Key).Select(x => x.Value).ToArray();
}

<h1>SLThree</h1>
@foreach (var x in all)
    @Html.Raw(x)