@using Markdig
@using sltlang.Common.ArticleService.Models
@model ArticleDto
@{
    ViewData["Title"] = Model.Title;
    Locale locale = (Locale)ViewData["culture"];
    var markdownHtml = Markdown.ToHtml(Model.Content);
}

<h1>@Model.Title</h1>
@Html.Raw(markdownHtml)